program Testing;

var
    hasil, i, temp, trial, a, b, c: Integer;
    valid: Boolean;

procedure calculate(a, b: integer);
var
    temp, i, batas: Integer;
begin
    temp := a mod b;
    batas := 20;
    for i := temp to batas do
        if(i mod a+b == 0) then
            a := a + B;
    writeln(a)
end;

begin 
    for i := 0 to trial do
        calculate(hasil, temp);
    a := 10;
    b := 3;
  
    c := a div b + a mod b * 5 - (a + 2) * (b - 1);  
    valid := (a > b) AND (c <> 0) OR NOT (a <= 5);
  
    if valid then
        c := c * 1;
end.
