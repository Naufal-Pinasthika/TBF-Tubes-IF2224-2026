program testingagain;

const
  PI == 3.14;
  NumOfiterator == 100;
  Pesan == 'Selamat Datang';
  BeeMovieScript == 'According to all all law';

var
  area, r: Real;
  hate: Boolean;

begin
  r := 12.7;
  area := PI * (r*r);
  hate := False;
  writeln(BeeMovieScript);
  writeln('Area of circle: ');
  writeln(area)
end.